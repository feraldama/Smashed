# Específico de @smash/api — los valores reales se toman del .env raíz vía dotenv.
# Este archivo existe sólo como referencia. Prisma CLI lee DATABASE_URL del .env raíz.
DATABASE_URL=postgresql://postgres:12345@localhost:5432/smash?schema=public
