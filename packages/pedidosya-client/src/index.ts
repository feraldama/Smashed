/**
 * @smash/pedidosya-client — placeholder.
 *
 * Cliente para integración con PedidosYa (recepción de pedidos delivery).
 * Se implementa en Fase 3 cuando estén las credenciales.
 *
 * Operaciones esperadas:
 *  - verifyWebhookSignature(payload, signature)
 *  - acceptOrder(orderId)
 *  - updateOrderStatus(orderId, status)
 *  - syncMenuAvailability(productos)
 *  - syncStoreHours(sucursal)
 */

export const PEDIDOSYA_PLACEHOLDER = true;
